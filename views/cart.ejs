<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Корзина</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <h1>Корзина</h1>
    <% if (cart.length > 0) { %>
      <div class="cart-items">
        <% cart.forEach(item => { %>
          <div class="cart-item">
            <h2><%= item.name %></h2>
            <p>Цена: <%= item.price %> руб.</p>
            <p>Количество: <%= item.quantity %></p>
            <p>Размер: <%= item.size %></p>
            <p>Цвет: <%= item.color %></p>
            <button onclick="removeFromCart('<%= item.id %>', '<%= item.size %>', '<%= item.color %>')">Удалить</button>
          </div>
        <% }) %>
      </div>
      <div class="delivery-options">
        <button onclick="selectDelivery('pickup')">Самовывоз</button>
        <button onclick="selectDelivery('delivery')">Доставка</button>
      </div>
      <button onclick="placeOrder()">Оформить заказ</button>
    <% } else { %>
      <p>Корзина пуста</p>
    <% } %>
  </main>
  <%- include('partials/footer') %>
  <script src="/js/main.js"></script>
</body>
</html>